{
    "name": "vistas-monorepo",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "client",
        "server"
    ],
    "scripts": {
        "build": "npm run build --workspace=client",
        "postinstall": "npx prisma@6 generate --schema=server/prisma/schema.prisma"
    },
    "dependencies": {
        "prisma": "^6.19.2",
        "@prisma/client": "^6.19.2"
    }
}